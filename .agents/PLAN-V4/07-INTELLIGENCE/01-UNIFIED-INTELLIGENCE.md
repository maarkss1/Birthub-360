# Unified Intelligence

Canonical route:

`/app/intelligence`

Canonical facade:

`IntelligenceGateway` ou equivalente aprovado.

Normalizar:

- provider/model;
- prompts;
- agents;
- RAG;
- guardrails;
- logging;
- cost/token telemetry;
- memory;
- governance.

Existing AILog, AgentMemory and AIGuardrailEvent devem ser reutilizados.

Não reabrir correções de RLS já comprovadas sem regressão.
