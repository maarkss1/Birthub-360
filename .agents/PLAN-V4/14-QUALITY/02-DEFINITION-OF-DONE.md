# Definition of Done

Implementation
+ relevant tests
+ runtime evidence
+ tenant/security review
+ API/schema docs where relevant
+ no orphan dependency
+ rollback
+ gate update.

Security requires negative runtime tests whenever behavior is testable.

Product capabilities additionally require:

- persona;
- pillar;
- UX entry point;
- reason for navigation visibility;
- contextual alternative where applicable.
